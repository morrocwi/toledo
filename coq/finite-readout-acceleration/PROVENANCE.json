{
  "source": "finite-readout-acceleration",
  "upstream_repo": "https://github.com/morrocwi/finite-readout-acceleration (public)",
  "upstream_commit": "f7fbc318eb104314cbc81d9f718a9a03abaadc05",
  "licence": "MIT License (LICENSE, repo root) -- Copyright (c) 2026 Yaoharee Lahtee",
  "licence_note": "Public MIT-licensed repository. Copied verbatim (byte-for-byte diff against the upstream commit above is empty; git hash-object on the copied file matches both the manifest's recorded blob and the upstream working tree's current blob exactly, all three re-verified in this pass) under its own MIT terms -- no additional authorisation needed beyond the licence itself. Toledo's coq/ tree is also MIT-licensed, so this import is licence-compatible. Only the manifest's declared scope for this source (the 1 .v file under formal/) was copied; README.md, DESIGN_NOTES.md, CHANGELOG.md, CONTRIBUTING.md, fra/ (the Python package), tests/, examples/, and build/ were not imported -- out of scope (non-Coq).",
  "files": [
    {
      "path": "formal/FRA_Closures.v",
      "blob": "225a889a8ea0fa3bdfac4b0ce7cf14cbbe1ed280",
      "licence": "MIT License (LICENSE, repo root) -- Copyright (c) 2026 Yaoharee Lahtee",
      "theorem_count": 6
    }
  ],
  "copied_at": "2026-09-06T20:32:00Z",
  "build_note": "Toledo-side additions only, per T10.3/task rule (a Toledo-side wrapper for path/-Q flags is allowed; upstream files never edited): _CoqProject (`-Q formal FRA`) written on the Toledo side (upstream's own formal/verify.sh compiles the file directly with `coqc -q FRA_Closures.v`, no logical qualification and no _CoqProject/Makefile at all -- the -Q wrapper here is Toledo's own path-management addition, not a copy of anything upstream). Makefile regenerated from that _CoqProject via `coq_makefile -f _CoqProject -o Makefile` (gitignored, not committed). Built with `make` (no -j) inside coq/finite-readout-acceleration -- RAM checked via `free -g` before the build (docs/RAM_LOW absent both before coqdep/coqc and before the independent Print-Assumptions scratch-file pass; ~7 GB available, well above the 2 GB floor), one coqc process at a time throughout. `COQC formal/FRA_Closures.v` succeeded with exit 0; the file's own 5 trailing `Print Assumptions` calls (for the 5 Theorem-keyword results) each printed 'Closed under the global context' in upstream's declared order.",
  "verification_note": "Every Theorem/Lemma/Corollary/Proposition listed in registry/coq_imports.json for this source (6 total: the field lemma Qdiv_cross_lt plus the 5 Theorem-keyword results break_even_iff, ceiling_strict, injective_key_slowdown, hit_rate_bounds, bottleneck_ceiling) was independently re-checked via one scratch file (`From FRA Require Import FRA_Closures. Print Assumptions <name>.` for all 6 names, one coqc process, `-Q . FRA`), separate from FRA_Closures.v's own 5 trailing Print Assumptions calls -- a strict superset (adds the one Lemma the manifest names but the source file's own trailing block does not itself re-print). Result: all 6 manifest-listed identifiers independently print exactly 'Closed under the global context' -- axiom-free, verified on this machine at build time, not merely claimed by upstream. This matches upstream's own repo-level claim (README.md badge + prose: 'Coq 8.20 . 5 theorems axiom-free' counting only the Theorem-keyword results) but the manifest's own file-level extraction records 6 Theorem/Lemma/Corollary/Proposition names for this file (the extra one being the Qdiv_cross_lt field Lemma) -- a discrepancy the manifest itself already flagged as 'not reconciled here'; this pass independently confirms axiom-freedom for all 6 of the manifest's names rather than only upstream's narrower 5-item headline count, so the discrepancy is resolved in the safe direction (checked a superset, found nothing unaccounted for) without silently adopting either count as authoritative.",
  "scratch_file_cleanup": "The scratch Print-Assumptions file (formal/scratch_print_assumptions.v) and all of its build byproducts (.vo/.vok/.vos/.glob/.aux) were deleted after the check ran; only formal/FRA_Closures.v (and its own build byproducts, gitignored) remain on disk.",
  "redactions": [
    {
      "found": "one local workstation home-directory filesystem path, inside a transient Coq-generated build artifact (formal/.FRA_Closures.aux -- an auxiliary file coqc writes recording the compiled file's own local path)",
      "action": "no redaction needed in any committed file -- this artifact is a build byproduct, not source content; it is covered by toledo/.gitignore (`.*.aux`, `*.aux`, alongside `*.vo`/`*.vok`/`*.vos`/`*.glob`/`Makefile`/`Makefile.conf`), so it is never staged or committed and never reaches the public repository. Re-confirmed by `git status --short` on this directory: only the source/report files (this file, VERIFY_REPORT.md, verify_report.json, LICENSE.upstream, _CoqProject, formal/FRA_Closures.v) are untracked/to-be-added; no build artifact appears.",
      "scope_note": "grep for local filesystem paths and the local username was run over every committable file in this tree (source, LICENSE.upstream, this file, VERIFY_REPORT.md, verify_report.json); the only hit was the gitignored .aux artifact above, and that hit is described here without reproducing the literal path. No hit for any private-repo-name pattern (the private solver arc is not referenced anywhere in this source's files)."
    }
  ]
}

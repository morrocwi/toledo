{
  "schema_version": "1.0",
  "reviewed_at": "2026-09-20",
  "scope": "Tooling-only change, no new theorem/equation content: (1) new scripts/compute_counts.py, the ONE script permitted to write registry/CANONICAL.json's top-level `counts` block, mirroring scripts/compute_resistance.py and scripts/compute_executable.py's own in-place-write discipline (reads canonical[]'s status/domain/tier/coq.coq_status fields, group-by's each, writes the `counts` key IN PLACE, touches no other field); wired into the Makefile's `build` target in the same relative position as `resistance`/`executable` (runs before `build`). Ran it for real (not just --dry-run): the numeric counts (entries/by_status/by_domain/by_tier/by_coq_status) are byte-identical to the prior hand-computed block -- only the free-text `counts.computed` provenance sentence changed, from a 2026-09-19 manual note to naming this script as the source of truth going forward. (2) AGENTS.md's Toledo-lookup-first gate now names the actual, verified-working first-move command for the 'Toledo lookup' step -- `python3 -m toledo_mcp.cli check --formula \"<statement text>\" ` (or `--code <exact code>`), run from `mcp/` -- functionally equivalent to the MCP-tool-call form `toledo_check({'code': ...})`/`toledo_check({'formula': ...})` that mcp/BENCHMARKS.md itself records (the literal CLI string is not itself quoted there; the CLI subcommand wraps the same underlying check), and mcp/toledo_mcp/cli.py's `check` subcommand (marked `FOUNDER RULE` in its own --help text), and actually executed here (returned a real NOT_REGISTERED verdict for an unregistered probe formula). Honest note: the task instruction assumed an existing 'a keyword hit is not a match, read the full statement' sentence already sat next to TG-RFG-01 in this repo's AGENTS.md, to be kept unchanged immediately after the new line -- a repo-wide grep found no such sentence anywhere in AGENTS.md or EQUATION_SOURCE_POLICY.md before this change (that rule exists in this workspace's global CLAUDE.md reuse-pipeline policy, not as prior text in this file); rather than fabricate a false 'kept unchanged' claim, that clause was added fresh, immediately following the new command line, in the same edit. (3) tests/test_registry.py already contained `test_counts_match_canonical` (recomputes the same group-by fresh from canonical[] and asserts it matches the stored counts block field-for-field) and mcp/tests/test_integration.py already contained the equivalent `test_counts_matches_registry_canonical_json_own_counts_field` -- both pre-existing, both re-verified passing after this change, no new duplicate test added (the task's own 'do not duplicate logic' instruction, satisfied by recognizing the required coverage was already complete rather than writing a third copy of the same check).",
  "todo": {
    "status": "reviewed-no-change",
    "evidence": "Tooling/doc change only; opens no research TODO item. The registry's own counts block is now maintained by a checked-in script instead of a hand/manual recompute, closing a latent staleness risk (tests/test_registry.py's own test_counts_match_canonical comment already warned about this class of drift) -- nothing further tracked."
  },
  "toledo": {
    "status": "updated",
    "evidence": "registry/CANONICAL.json's top-level `counts` block is now written by the new checked-in scripts/compute_counts.py instead of by hand -- this is a real update to the registry file's own provenance metadata (the counts.computed string now names the script as the source of truth), even though the numeric counts themselves are unchanged (confirmed byte-identical by a real, non-dry-run run of the new script against the prior hand-computed block). No tier/status/parent change to any equation entry; no new Toledo entry."
  },
  "claim_effect": "provenance-change"
}
